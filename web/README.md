# Web Assets

This directory is reserved for the northbound UI.

Suggested layout:

- `public/` for static files like JavaScript, CSS, images, and fonts
- `views/` for HTML templates if the UI is server-rendered

Keep browser-facing assets here so the northbound interface has a clear home in the repo.
